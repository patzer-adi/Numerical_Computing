#ifndef MENU_HPP
#define MENU_HPP

// Application controller layer
// All menu display, user choices, and action routing lives here.
// The core library (Matrix, Solvers) has NO user interaction.
// The utils (Input, Display) handle raw I/O only.
// This file ties them together.

void runMenu();

#endif
